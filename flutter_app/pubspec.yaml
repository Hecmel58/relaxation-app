name: relaxation_flutter
description: Relaxation app (skeleton) - connects to the same backend API and WebSocket signaling
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ">=2.18.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  http: ^0.13.6
  flutter_secure_storage: ^8.0.0
  web_socket_channel: ^2.2.0
  flutter_webrtc: ^0.9.20
  provider: ^6.0.5
  fl_chart: ^0.55.1
  table_calendar: ^3.0.10

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
